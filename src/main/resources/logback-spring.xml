<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console output for local development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logstash appender for sending logs to ELK stack -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>localhost:5044</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"app_name":"kata-bankAccount-back"}</customFields>
        </encoder>
        <!-- Reconnection settings -->
        <reconnectionDelay>10 seconds</reconnectionDelay>
        <includeCallerData>true</includeCallerData>
    </appender>
    
    <!-- Async appender to prevent blocking if Logstash is down -->
    <appender name="ASYNC_LOGSTASH" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="LOGSTASH" />
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ASYNC_LOGSTASH" />
    </root>

    <!-- Application-specific logging -->
    <logger name="com.kata.bankaccountback" level="DEBUG" />
    
    <!-- Reduce noise from Spring framework -->
    <logger name="org.springframework" level="INFO" />
    <logger name="org.hibernate" level="WARN" />
</configuration>